# Frontend environment (copy to frontend/.env.local)
NEXT_PUBLIC_API_URL=http://localhost:5001

# Production (Cloudflare Pages dashboard → Settings → Environment variables):
# NEXT_PUBLIC_API_URL=https://magil-clinic-api.onrender.com
#
# Rebuild/redeploy Pages after changing this value — it is baked in at build time.
